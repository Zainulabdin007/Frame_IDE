# Frame Inference Runtime

**Superseded by** [`FRAME_LOCAL_RUNTIME_ARCHITECTURE.md`](./FRAME_LOCAL_RUNTIME_ARCHITECTURE.md).

That document covers the runtime boundary, `IFrameRuntimeService` registry, stub / MLX / llama.cpp placeholders, `.frame/config/runtime.json`, privacy, and model ownership (user-provided weights only).
